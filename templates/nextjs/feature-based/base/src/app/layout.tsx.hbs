{{#if tailwind}}import './globals.css'{{/if}}
{{#if tanstackQuery}}import QueryProvider from '../components/providers/QueryProvider'{{/if}}

export const metadata = {
  title: '{{projectName}}',
  description: 'Generated by CLI Scaffolding Tool',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
{{#if tanstackQuery}}
        <QueryProvider>
          {children}
        </QueryProvider>
{{else}}
        {children}
{{/if}}
      </body>
    </html>
  )
}