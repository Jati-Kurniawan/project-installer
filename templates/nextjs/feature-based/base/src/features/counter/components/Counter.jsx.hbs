{{#if zustand}}import { useCounterStore } from '../hooks/useCounter'{{else}}import { useCounter } from '../hooks/useCounter'{{/if}}
import Button from '../../../components/ui/Button'

export default function Counter() {
{{#if zustand}}
  const { count, increment, decrement, reset } = useCounterStore()
{{else}}
  const { count, increment, decrement, reset } = useCounter()
{{/if}}

  return (
    <div className="bg-white p-6 rounded-lg shadow-md max-w-md mx-auto">
      <h2 className="text-2xl font-bold text-center mb-4">Counter Example</h2>
      <div className="text-center">
        <div className="text-4xl font-bold text-blue-600 mb-6">{count}</div>
        <div className="flex gap-2 justify-center">
          <Button onClick={decrement} variant="outline" size="sm">
            -1
          </Button>
          <Button onClick={increment} size="sm">
            +1
          </Button>
          <Button onClick={reset} variant="secondary" size="sm">
            Reset
          </Button>
        </div>
      </div>
    </div>
  )
}